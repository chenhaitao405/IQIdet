# 项目进度

> 最后更新：2026-05-30

## 当前阶段

**Phase 1：单丝像质计等级识别** ✅ 已完成

- 交付入口 `run_iqi_grade_infer.py` 可产出一致性 JSON
- FClip 丝数推理、YOLO-OBB ROI 检测、PaddleOCR 全图/ROI OCR 均已集成
- 区域 OCR 和区域 SNR 旁路 API 可用
- 详细交付说明见 `docs/README_IQI_GRADE_INFERENCE_DELIVERY.md`

## 下一阶段

**Phase 2：双丝像质计分辨率计算** 🔜 即将开始

### 需求文档

- 客户需求：`docs/需求/双丝像质计分辨率.md`
- 标准解读：`docs/contract/JBT7902-2025_双丝型像质计.md`
- 标准原文：`docs/contract/JBT 7902-2025《无损检测 丝型像质计通用规范》.pdf`
- 双丝型输出的是**不清晰度值**（mm），不是像质计等级，与单丝型走独立处理通道。

### 核心公式

$$Contrast = \frac{a + b - 2c}{a + b}$$

- a, b：两个黑区灰度均值（正片）或两个白区灰度均值（负片）
- c：中间白区灰度均值（正片）或中间黑区灰度均值（负片）
- 阈值：Contrast < 20% 视为不可分辨
- 遍历方向：从最粗组（D1）到最细组，找到**第一个** Contrast < 20% 的组

### 架构分工

| 模块 | 职责 | 状态 |
|------|------|------|
| OBB 检测模型 | 双丝 ROI 粗定位 + 方向（长边 = 剖面线方向） | 待开发 |
| 剖面线提取 | 沿 OBB 长边方向提取灰度剖面 | Demo 阶段 |
| 波峰两波谷算法 | 从一维剖面信号中定位 a、b、c，计算 Contrast | Demo 阶段 |
| 遍历判定 | 从粗到细找到第一个 Contrast < 20% 的组 | Demo 阶段 |
| 查表输出 | 组编号 → 不清晰度物理值（mm） | Demo 阶段 |

### Demo 第一阶段

**前提**：假设已通过 OBB 或手动标注获取了剖面线（一维灰度信号）。

**输入**：每组线对的剖面灰度曲线
**输出**：第一个 Contrast < 20% 的组编号

**实现路线**：

```
输入：一组剖面灰度值 array
  ↓
平滑去噪（可选，视图像质量）
  ↓
定位两个极值点（峰值或谷值）→ a、b
定位中间的相反极值点 → c
  ↓
计算 Contrast = (a + b - 2c) / (a + b)
  ↓
遍历所有组，找到第一个 Contrast < 20% 的组
  ↓
输出组编号 + 查表不清晰度值
```

**核心难点 —— 从一维含噪信号中准确找到 a、b、c**：

1. **a、b 取峰值点还是峰值邻域均值？** 邻域均值抗噪更好，窗口大小需要实验确定。
2. **正/负片判定**：D1（最粗组）信号方向确定后，后续所有组一致。只需在 D1 上判断一次：
   - 正片：两黑夹一白 → 剖面曲线为"峰-谷-峰"
   - 负片：两白夹一黑 → 剖面曲线为"谷-峰-谷"

**验证标准**：
- 在已知答案的标定图像上，输出的第一个不可分辨组与人工判断一致
- 正片/负片两种图像类型均能正确判定

### 已知约束

- 双丝图像以 `GB`/`ISO` 文件名前缀区分
- 团标即将发布，双丝组数可能从 13 组扩展到 15-18 组，需支持分段处理
- 非分段双丝：所有组 Contrast 均 > 20% 时返回最细组（超出量程上限）

### 待确认事项

- [ ] 客户团标的完整查表映射（D 编号 ↔ 丝径 mm），确认是否完全遵循 JBT 7902-2025 Table 2
- [ ] 输出语义：第一个**不可分辨**的组 vs 最后一个**可分辨**的组（差 1 个位置）
- [ ] HR4 分段双丝像质计的线对编号规则
- [ ] 客户文档中"第 8 组对应 0.6mm"与 JBT 7902-2025 Table 2 不一致（D8 = 0.16mm），需确认以哪个查表为准

## 已完成的前置工作

- `scripts/tool/filter_nocrack_no_roi.py`：从 nocrack 数据集中筛选出未检测到单丝 ROI 的图像（即候选双丝像质计）
- 候选图像已保存到 `outputs/候选双丝像质计/`（311 张）
- 这些图像可用于双丝像质计定位和分辨率计算的开发和验证

## 已知问题 / 待修复

- `TODO.md` 中待办事项待整理

## 文件定位速查

| 目的 | 路径 |
|------|------|
| 代码架构 | `ARCHITECTURE.md` |
| 交付说明 | `docs/README_IQI_GRADE_INFERENCE_DELIVERY.md` |
| 模型资产 | `docs/MODEL_ASSETS.md` |
| 单丝等级规则契约 | `docs/contract/IQI_SINGLE_WIRE_MARKER_GRADE_RULE.md` |
| 双丝需求 | `docs/需求/双丝像质计分辨率.md` |
| 双丝标准解读 | `docs/contract/JBT7902-2025_双丝型像质计.md` |
| 待办事项 | `TODO.md` |
| Agent 指南 | `AGENTS.md` |
